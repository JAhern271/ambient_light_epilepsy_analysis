# Results — 2026-03-18

> **Provenance uncertain.** These files predate provenance stamping. The date is the last
> commit that touched them, not necessarily the date they were generated, and the exact
> code version that produced them is not recorded. Treat the values as current but do not
> assume they can be regenerated bit-for-bit. Results produced after 2026-08-17 carry a
> `provenance.json` sidecar recording the commit, machine and parameters.

## Files

| File | Contents |
|---|---|
| `lux_5min_fmatch_analysis.csv` | Per-participant light metrics plus covariates, both cohorts, PWE and matched controls. 861 rows. The main analysis table. |
| `pwe_lux_summary_{G,H}.csv` | Light metrics for PWE only, per cycle. Earlier intermediate form, no covariates. |
| `control_lux_summary_{G,H}.csv` | Light metrics for matched controls only, per cycle. Earlier intermediate form. |

## What is known

- Computed from the **5-minute downsampled** LUX data.
- Generated by `scripts/lux_analysis.py` or the equivalent notebook code of the time.
- Postdate the 2026-03-11 changes to `time_above_threshold` (no longer averaged across
  days) and `relative_amplitude` (M10/L5 midpoints added), so those columns are on the
  current definitions.
- Verified on 2026-08-17: rerunning the current code for 12 participants reproduces these
  values to within 1e-9, so the pipeline still matches what produced them.

## Known duplicate, resolved

This table previously existed twice, as `analysis/lux_5min_fmatch_analysis.csv` and
`analysis/lux/lux_5min_fmatch_analysis.csv`, differing only in column order. They were
confirmed numerically identical and the redundant copy was deleted on 2026-08-17.

## Not included here

The 1 Hz analysis (`lux_1hz_fmatch_analysis.csv`), which notebook 08 currently reads,
lives on the W: drive and was produced on BlueBEAR. It is not committed because no script
in this repository regenerates it.
